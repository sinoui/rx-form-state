---
name: useFormStateContext
route: /api-use-form-state-context
---

# useFormStateContext

从上下文中获取表单状态的 hook。

```ts
import { useFormStateContext } from '@sinoui/rx-form-state';

function Demo() {
  const formState = useFormStateContext();

  // ...
}
```

## 关联

- [FormStateContext](api-form-state-context)
